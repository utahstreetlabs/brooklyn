<%= word_wrap %Q[#{raw @commenter.firstname},

#{raw @replier.name} just replied to your comment on #{raw @listing.title}.

#{mailer_format_text @comment.text}

#{raw @replier.firstname} said:

#{mailer_format_text @reply.text}

Keep the conversation going.

Reply to #{raw @replier.firstname} at #{rewrite_mailer_url listing_url(@listing)}] %>

<%= render '/shared_mailer/footer' %>