<%= render '/shared_mailer/header' %>

<%= mailer_graf do %>
  You should have received your purchase of <%= @order.listing.title %>.  Remember to take a look at the item in the next <%= tx_review_period_in_words @order %> to ensure that everything is as described.  If there is a problem, please <%= mailer_link_to 'notify us', listing_url(@order.listing) %>.  We will follow our <%= mailer_link_to 'Transaction Policy', 'http://corporate.copious.com/transaction-policy' %> to address any issues.
<% end %>

<%= mailer_graf do %>
  <%= mailer_link_to 'If everything is OK, ensure the seller is paid quickly by completing the order.', listing_url(@order.listing) %>
<% end %>

<%= render 'buyer_order_details', order: @order %>

<%= mailer_subheader do %>
  What else?
<% end %>

<ol style="padding-left: 16px;" class="ordered-list">
  <li>
    <%= mailer_graf do %>
      Try <%= mailer_link_to "following #{@order.listing.seller.name}", public_profile_url(@order.listing.seller) %>, so you can get a first look at what they have to sell in the future.
    <% end %>
  </li>
  <li>
    <%= mailer_graf do %>
      Take the plunge and <%= mailer_link_to "sell something yourself!", dashboard_url %> It's easy, safe and fun, and only takes a couple of minutes.
    <% end %>
  </li>
</ol>

<%= render '/shared_mailer/footer' %>
