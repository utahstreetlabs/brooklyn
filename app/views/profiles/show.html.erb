<%
title "User Profile for #{profile_user.display_name}: Listings"
javascript 'profile_show'
fb_head_prefix
%>

<%= profile_banner @profile_user, class: 'kill-margin-left' %>

<%= render 'listings', results: results, empty_msg: "#{profile_user.firstname} has not listed any items yet." %>
